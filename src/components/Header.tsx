import React from 'react';
import '../style/Header.scss';

const Header = () => {
    return (
        <div className='header-wrap'>
            <span>HEADER</span>
        </div>
    );
};

export default Header;